// __STUB__ — placeholder so the v2 repo layout exists (UPGRADE_PLAN Phase 0).
// The real firmware v2 core (GPIO mains, state machine, actuation) lands in Phase 1.
// deploy/ota-esp32.sh refuses to build/OTA a stub.

#include <Arduino.h>

void setup() {}
void loop() {}
