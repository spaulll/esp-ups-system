#!/usr/bin/env python3
# __STUB__ — placeholder so the v2 repo layout exists (UPGRADE_PLAN Phase 0).
# The real Pi brain (Telegram + reconciler + webhook receiver) lands in Phase 2.
# deploy/deploy-pi.sh refuses to push stubs to the live Pi.
